    var a = -10;
    var b = -16;
    
    console.log(checkAB(a,b));

    
function checkAB(a,b)
{
    if (a>=0 && b>=0)
    {
        return (a-b);
    }
    else if (a<0 && b<0)
    {
        return (a*b);    
    }
    else
    {
        return (a+b);    
    }
}
